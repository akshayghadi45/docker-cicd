# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      #run: mvn -B package --file pom.xml
      run: mvn clean test

#    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
#    - name: Update dependency graph
#      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6

#name: Java CI with Maven
#
#on:
#  push:
#    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]
#
#jobs:
#  build:
#
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up JDK 17
#        uses: actions/setup-java@v3
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Install Chrome & dependencies
#        run: |
#          sudo apt-get update
#          sudo apt-get install -y \
#            wget \
#            unzip \
#            xvfb \
#            libnss3 \
#            libxss1 \
#            libasound2 \
#            fonts-liberation \
#            libatk-bridge2.0-0 \
#            libgtk-3-0 \
#            libgbm-dev \
#            libdrm2 \
#            xdg-utils \
#            libx11-xcb1 \
#            libxcomposite1 \
#            libxdamage1 \
#            libxrandr2
#          # Install latest Chrome
#          wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
#          sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get install -f -y
#
#      - name: Setup WebDriverManager
#        run: |
#          # ChromeDriver will be managed automatically by WebDriverManager in your code
#          echo "WebDriverManager will download ChromeDriver automatically"
#
#      - name: Run Maven tests
#        run: mvn clean test -B -Dplatform=remote_git
